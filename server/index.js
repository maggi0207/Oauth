I've been investigating the UI lag issue where typing in an input and immediately clicking another control (button, Date Picker, Cancel, etc.) causes the UI to freeze for 4–5 seconds before updating. To isolate the issue, I removed all business logic and reduced the page to a simple input and button. The issue still occurred. I then created a fresh Vite + React application with only an input and button, and the same behavior was reproduced. I also created a fresh Angular application with the same result, which rules out our application code and the React framework. I tested the applications in different environments and found that they work normally in the VS Code built-in browser, but the lag consistently occurs in Chrome and Edge. Based on the investigation, this does not appear to be an issue with our project code or the React framework. The issue is reproducible in both React and Angular applications and only occurs in Chrome/Edge, while the same applications work correctly in the VS Code built-in browser. This points to a browser or corporate environment issue rather than an application issue.
